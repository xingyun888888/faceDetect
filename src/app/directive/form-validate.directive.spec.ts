import { FormValidateDirective } from './form-validate.directive';

describe('FormValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new FormValidateDirective();
    expect(directive).toBeTruthy();
  });
});
