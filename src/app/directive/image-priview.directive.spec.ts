import { ImagePriviewDirective } from './image-priview.directive';

describe('ImagePriviewDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagePriviewDirective();
    expect(directive).toBeTruthy();
  });
});
