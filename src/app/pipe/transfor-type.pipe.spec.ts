import { TransforTypePipe } from './transfor-type.pipe';

describe('TransforTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TransforTypePipe();
    expect(pipe).toBeTruthy();
  });
});
